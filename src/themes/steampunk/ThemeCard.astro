---
// src/themes/steampunk/ThemeCard.astro
import meta from './theme.json';

interface Props {
  isActive?: boolean;
}

const { isActive = false } = Astro.props;
---

<article class:list={['theme-card', 'theme-card-steam', { 'is-active': isActive }]}>
  <div class="theme-preview-container">
    <div class="theme-iframe-wrap">
      <iframe data-src="/t/steampunk" title="Miniatura ao vivo do tema Steampunk" class="theme-live-iframe" loading="lazy" tabindex="-1" aria-hidden="true"></iframe>
    </div>
    <div class="theme-static-wrap">
      <img src="/images/themes/steampunk.png" alt="Screenshot do tema Steampunk" class="theme-static-img" loading="lazy" />
    </div>
    <div class="preview-overlay-badge">
      <span class="swatch-dot" style="background: #c98a3e;"></span>
      <span>{meta.name}</span>
    </div>
  </div>

  <div class="card-body">
    <div class="card-header">
      <div class="card-title-group">
        <h3 class="card-title">{meta.name}</h3>
        <span class="card-tagline">{meta.tagline}</span>
      </div>
      {isActive && <span class="badge-active">Tema Ativo</span>}
    </div>
    <p class="card-desc">{meta.description}</p>
    <div class="card-footer">
      <a href="/t/steampunk" class="btn-theme-select">
        {isActive ? 'Ver páginas neste tema →' : 'Ver o site inteiro assim →'}
      </a>
    </div>
  </div>
</article>

<style>
  .theme-card-steam {
    background: #3a2410;
    border: 1px solid #5a3d1c;
    border-radius: 6px;
    overflow: hidden;
    display: flex;
    flex-direction: column;
    box-shadow: 0 6px 20px rgba(0, 0, 0, 0.4);
    transition: border-color 0.2s ease, transform 0.2s ease, box-shadow 0.2s ease;
  }

  .theme-card-steam:hover {
    border-color: #c98a3e;
    transform: translateY(-3px);
    box-shadow: 0 10px 24px rgba(201, 138, 62, 0.25);
  }

  .theme-card-steam.is-active {
    border-color: #c98a3e;
    box-shadow: 0 0 0 2px rgba(201, 138, 62, 0.45);
  }

  .theme-preview-container {
    width: 100%;
    height: 220px;
    position: relative;
    background-color: #241708;
    overflow: hidden;
    border-bottom: 1px solid #5a3d1c;
  }

  .theme-iframe-wrap { width: 100%; height: 100%; position: absolute; inset: 0; overflow: hidden; pointer-events: none; }
  .theme-live-iframe { width: 1200px; height: 800px; border: none; transform-origin: 0 0; transform: scale(0.35); pointer-events: none; }
  .theme-static-wrap { display: none; width: 100%; height: 100%; }
  .theme-static-img { width: 100%; height: 100%; object-fit: cover; object-position: top; display: block; }

  @media (max-width: 768px) {
    .theme-iframe-wrap { display: none !important; }
    .theme-static-wrap { display: block !important; }
  }

  .preview-overlay-badge {
    position: absolute;
    bottom: 0.75rem;
    left: 0.75rem;
    background: rgba(36, 23, 8, 0.9);
    border: 1px solid rgba(201, 138, 62, 0.4);
    padding: 0.25rem 0.6rem;
    border-radius: 4px;
    font-family: 'Cinzel', serif;
    font-size: 0.65rem;
    color: #f2e6cf;
    display: flex;
    align-items: center;
    gap: 0.4rem;
    z-index: 2;
  }

  .swatch-dot { width: 8px; height: 8px; border-radius: 50%; }

  .card-body { padding: 1.5rem; display: flex; flex-direction: column; gap: 1rem; flex: 1; }
  .card-header { display: flex; justify-content: space-between; align-items: flex-start; gap: 0.5rem; }
  .card-title { font-family: 'Cinzel', serif; font-size: 1.05rem; color: #f2e6cf; margin: 0; }
  .card-tagline { font-family: 'EB Garamond', serif; font-size: 0.9rem; color: #c98a3e; display: block; margin-top: 0.4rem; }

  .badge-active {
    background: rgba(201, 138, 62, 0.2);
    border: 1px solid #c98a3e;
    color: #c98a3e;
    font-family: 'EB Garamond', serif;
    font-size: 0.7rem;
    font-weight: 700;
    text-transform: uppercase;
    padding: 0.2rem 0.5rem;
    border-radius: 4px;
  }

  .card-desc { color: #c2a377; font-size: 0.95rem; line-height: 1.55; margin: 0; flex: 1; font-family: 'EB Garamond', serif; }
  .card-footer { margin-top: auto; padding-top: 0.5rem; }

  .btn-theme-select {
    display: inline-block;
    width: 100%;
    text-align: center;
    background: rgba(201, 138, 62, 0.15);
    border: 1px solid #c98a3e;
    color: #c98a3e;
    font-family: 'EB Garamond', serif;
    font-size: 1rem;
    font-weight: 700;
    padding: 0.65rem 1rem;
    border-radius: 4px;
    text-decoration: none;
    transition: all 0.2s ease;
  }

  .btn-theme-select:hover {
    background-color: #c98a3e;
    color: #241708;
  }
</style>
